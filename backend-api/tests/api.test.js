const request = require("supertest");
const express = require("express");

describe("POST /generate", () => {
  it("debería retornar 202 Accepted cuando el JSON es válido", async () => {
    // Aquí simularemos una petición al servidor
  });

  it("debería fallar si no se envía template_type", async () => {
    // Test de validación de datos
  });
});
